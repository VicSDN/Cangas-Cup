---
export interface Props {
  year?: number;       
  className?: string;  
  text?: string;      
}

const { 
  year, 
  className = '', 
  text = "Editar Salón de los Elegidos" 
} = Astro.props;


const targetUrl = year 
  ? `/admin/hall-of-fame-editor?year=${year}` 
  : '/admin/hall-of-fame-editor'; 

---

<a
  href={targetUrl}
  class:list={[
    "inline-block px-6 m-4 py-3 text-sm font-semibold text-white rounded-lg shadow-md transition-colors duration-150 ease-in-out",
    "bg-sky-600 hover:bg-sky-700 focus:outline-none focus:ring-2 focus:ring-sky-500 focus:ring-offset-2 dark:focus:ring-offset-slate-800",
    "text-center", 
    className 
  ]}
  role="button"
>
  {text}
</a>